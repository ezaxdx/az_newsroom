// 기본 카테고리 (DB에 데이터 없을 때 폴백)
export const DEFAULT_CATEGORIES = ["AI", "MICE", "TOURISM"];
export const DEFAULT_NAV_CATEGORIES = DEFAULT_CATEGORIES;

// 하위 호환성 (기존 코드에서 참조 중인 경우)
export const ALL_CATEGORIES = DEFAULT_CATEGORIES;
